{{>layout/header}}

<h1>AlgoRank V1</h1>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/problem/save" role="button" class="btn btn-primary">글 등록</a>
            {{#memberName}}
                Logged in as: <span id="member">{{memberName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
            {{/memberName}}
            {{^memberName}}
<!--                <a href="/login" role="button" class="btn btn-primary"> 회원가입 </a>-->
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
<!--                <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a>-->
            {{/memberName}}
        </div>
    </div>
    <br>
    <!-- 목록 출력 영역 -->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>번호</th>
            <th>이름</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#members}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/member/{{id}}">{{name}}</td>
            </tr>
        {{/members}}
        </tbody>
    </table>
</div>

{{>layout/footer}}